export type Product = { id: string; slug: string; title: string; description: string; price: number; saleActive: boolean; salePrice: number | null; category: string; tags: string[]; image: string; inventory: number; active: boolean; featured?: boolean; };
export type CartItemInput = { productId: string; quantity: number };
export type OrderStatus = 'pending_payment' | 'paid' | 'fulfilled';
export type Order = { id: string; email: string; customerName?: string; shippingAddress?: string; items: Array<{ productId: string; title: string; price: number; quantity: number }>; subtotal: number; shippingCost: number; total: number; status: OrderStatus; createdAt: string; };
export type CheckoutInput = { email: string; customerName: string; shippingAddress: string; items: CartItemInput[] };
export type Store = { listProducts(): Promise<Product[]>; listAdminProducts(): Promise<Product[]>; getProduct(slug: string): Promise<Product | undefined>; upsertProduct(product: Product): Promise<Product>; updateProductImage(slug: string, image: string): Promise<Product | undefined>; listOrders(): Promise<Order[]>; listOrdersByEmail(email: string): Promise<Order[]>; getOrder(orderId: string): Promise<Order | undefined>; createOrder(input: CheckoutInput): Promise<Order>; markOrderPaid(orderId: string): Promise<Order | undefined>; markOrderFulfilled(orderId: string): Promise<Order | undefined>; };
